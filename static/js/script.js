(function() {
    "use strict";

    var config = {
        elements: {
        }
    };

    var app = {
        init: function() {
            console.log('app started :)');
        }
    };

    app.init();
})();
